# Phase v0.3 -- Execution Report

**Date:** 2026-07-02
**Branch:** main
**Label:** v0::version:0
**Target version:** 0.3.0 (not bumped -- release awaits explicit user confirmation)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Phase v0.3 (ASCII renderer and the light demo) is complete, and with it Layer 1 readiness for version v0: the renderer shows a living, moving light field, and total energy holds steadily at the intended level over the smoke run.

## Issues

| # | NADI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | NADI-009 | ASCII renderer | v0.3 | completed | 4e20187 | 3 | pass |
| 2 | NADI-010 | `demos/light.py` scenario | v0.3 | completed | 6e6a740 | 3 | pass |
| 3 | NADI-011 | Layer smoke run: the living light field | v0.3 | completed | c2fde7a | 2 | pass |

## Detailed Results

### NADI-009: ASCII renderer

**Status:** completed
**Commit:** 4e20187
**Files changed:**
- `nadiloka/render.py` (added: `BRIGHTNESS_RAMP`, total `glyph_for` mapping with clamping, `render_frame` producing the counter header over one glyph per cell)
- `tests/test_render.py` (added: total-mapping sweep with boundaries, out-of-range clamping, frame shape at arbitrary W x H, read-only invariant)
- `spec/ROADMAP.md` (checkbox)

**Validation:**
- [x] Unit + pinning tests: pass (44 passed)
- [x] Determinism harness: pass
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

---

### NADI-010: `demos/light.py` scenario

**Status:** completed
**Commit:** 6e6a740
**Files changed:**
- `demos/light.py` (added: the v0 demo -- ANSI-cleared frames at `--delay` pace or `--no-render` counter lines; required `--seed`; `run()` exposes the loop for tests)
- `tests/test_light_demo.py` (added: reproducibility, renderer on/off log equality, frames carrying counter headers)
- `spec/ROADMAP.md` (checkbox)

**Validation:**
- [x] Unit + pinning tests: pass (48 passed)
- [x] Determinism harness: pass
- [x] Smoke: the living light field renders in the terminal; `--no-render` emits clean counter lines
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

---

### NADI-011: Layer smoke run: the living light field

**Status:** completed
**Commit:** c2fde7a
**Files changed:**
- `tests/test_smoke_light.py` (added: the Layer 1 readiness gate -- 300 ticks at the reference parameters render every frame inside the energy band with the field visibly moving; two same-seed runs byte-identical in both counter logs and frames)
- `spec/ROADMAP.md` (checkbox)

**Validation:**
- [x] Unit + pinning tests: pass (50 passed, smoke included)
- [x] Determinism harness: pass
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

## Next Steps

- Release `0.3.0` via `/release-version 0.3.0` when you confirm.
- Next phase: v0.4 (Web visualization, React) -- upload with `/upload-issues spec/roadmap/implementation/v0.4-issues.md`, then execute NADI-012..NADI-015. Note v0.4 introduces the optional observation-layer dependencies (FastAPI/uvicorn, Node/Vite/React in `web/`).
