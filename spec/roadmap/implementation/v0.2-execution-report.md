# Phase v0.2 -- Execution Report

**Date:** 2026-07-02
**Branch:** main
**Label:** v0::version:0
**Target version:** 0.2.0 (bump confirmed by the user for this run)
**Executed by:** Claude Code

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Phase v0.2 (Space and the Tejas field) is complete. The v0.2 DoD holds: patches appear, live their lifetime, fade, and reappear elsewhere; total field energy fluctuates around a steady level (mean ~680 under the reference parameters) inside the configured 150..1500 band, with no runaway growth and no collapse.

## Issues

| # | NADI ID | Title | Phase | Status | Commit | Files | Tests |
|---|---------|-------|-------|--------|--------|-------|-------|
| 1 | NADI-005 | Grid and neighbourhood helpers | v0.2 | completed | a7fb497 | 4 | pass |
| 2 | NADI-006 | Tejas scalar field + `Patch` model | v0.2 | completed | 336b4ee | 4 | pass |
| 3 | NADI-007 | Patch lifecycle in the update-Tejas phase | v0.2 | completed | 34eb060 | 3 | pass |
| 4 | NADI-008 | Field-energy counter and energy-band test | v0.2 | completed | 39398ea | 5 | pass |

## Detailed Results

### NADI-005: Grid and neighbourhood helpers

**Status:** completed
**Commit:** a7fb497
**Files changed:**
- `nadiloka/grid.py` (added: bounded W x H `Grid`, explicit out-of-bounds errors, row-major iteration, Euclidean neighbourhood queries clipped at edges)
- `nadiloka/world.py` (modified: `width`/`height` in `WorldConfig`; `World` owns a `Grid`)
- `tests/test_grid.py` (added)
- `spec/ROADMAP.md` (checkbox)

A dedicated grid module (explicitly allowed by the issue) keeps imports acyclic: grid <- tejas <- world.

**Validation:**
- [x] Unit + pinning tests: pass (23 passed)
- [x] Determinism harness: pass
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

---

### NADI-006: Tejas scalar field + `Patch` model

**Status:** completed
**Commit:** 336b4ee
**Files changed:**
- `nadiloka/tejas.py` (added: `TejasField` with clamped illumination over clipped discs, the `take()` harvest seam for v1.2, `total_energy()`; `Patch` data model)
- `nadiloka/world.py` (modified: `food_max` + patch parameters in `WorldConfig`)
- `tests/test_tejas.py` (added)
- `spec/ROADMAP.md` (checkbox)

**Validation:**
- [x] Unit + pinning tests: pass (29 passed)
- [x] Determinism harness: pass
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass (FOOD_MAX clamp under overlap unit-tested)

---

### NADI-007: Patch lifecycle in the update-Tejas phase

**Status:** completed
**Commit:** 34eb060
**Files changed:**
- `nadiloka/world.py` (modified: `_update_tejas` drives age -> expire -> re-ignite -> rebuild; `_ignite` places patches via the World's single seeded RNG; initial batch staggered so energy holds a steady level)
- `tests/test_patch_lifecycle.py` (added)
- `spec/ROADMAP.md` (checkbox)

**Validation:**
- [x] Unit + pinning tests: pass (36 passed)
- [x] Determinism harness: pass
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass (same seed -> identical placements and lifecycle over 80 ticks)

---

### NADI-008: Field-energy counter and energy-band test

**Status:** completed
**Commit:** 39398ea
**Files changed:**
- `nadiloka/counters.py` (modified: the full v0 counter line -- tick, population, total field energy, live patches)
- `nadiloka/world.py` (modified: `energy_band_low=150`, `energy_band_high=1500`, `band_ticks=300` in `WorldConfig`, placed from measurement across five seeds: observed min 237, max 1179, mean ~680)
- `tests/test_energy_band.py` (added: the band guard + FOOD_MAX cap over the run)
- `tests/test_counters.py` (modified: new format)
- `spec/ROADMAP.md` (checkbox)

**Validation:**
- [x] Unit + pinning tests: pass (38 passed)
- [x] Determinism harness: pass with the new counters (byte-identical logs)
- [x] Smoke run: energy fluctuates around the intended level, patches hold at target
- [x] Lint (ruff): pass
- [x] Acceptance criteria: all pass

## Next Steps

- Release `0.2.0` (user-confirmed for this run), then upload and execute v0.3 (ASCII renderer and the light demo, NADI-009..NADI-011).
